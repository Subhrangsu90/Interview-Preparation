{
  "name": "interview-preparation",
  "version": "1.0.0",
  "private": true,
  "description": "Interview Preparation notes with web viewer and VS Code editor",
  "scripts": {
    "start": "cd web-viewer && node server.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "install-viewer": "cd web-viewer && npm install"
  },
  "devDependencies": {
    "vite": "^5.4.21"
  }
}
